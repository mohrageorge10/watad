class CacheKeys {
  static const String hasSeenOnboarding = 'has_seen_onboarding';
  // once the auth feature is done, so the splash check works without changes.
  static const String token = 'auth_token';
}